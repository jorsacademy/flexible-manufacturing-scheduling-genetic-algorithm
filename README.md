# Flexible Manufacturing Scheduling with a Genetic Algorithm

A reproducible Python implementation of a genetic algorithm for flexible parallel-machine manufacturing scheduling. Each job can have a different processing time on each compatible machine, and the optimizer minimizes makespan.

The repository includes automated tests and GitHub Actions CI.
